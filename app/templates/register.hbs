<h1>Register</h1>

<form {{action 'createUser' on='submit'}} class="well">
    <!--
    <div class="form-group">
        <label for="firstName">First Name</label>
        {{input value=firstName placeholder='John' class='form-control'}}
    </div>
    <div class="form-group">
        <label for="firstName">Last Name</label>
        {{input value=lastName placeholder='Doe' class='form-control'}}
    </div>
    <div class="form-group">
        <label for="email">Email address</label>
        {{input value=email placeholder='email@example.com' class='form-control'}}
    </div>
    <div class="form-group">
        <label for="password">Password</label>
        {{input value=password placeholder='Enter Password' class='form-control' type='password'}}
    </div>
    <button type="submit" class="btn btn-default">Register</button>
    -->
    <div class="form-group text-center">
        <button {{action 'login' 'google'}} class="btn btn-primary">Log in with Google</button>
    </div>
</form>
